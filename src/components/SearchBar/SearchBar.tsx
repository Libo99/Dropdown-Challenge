import React from 'react'

interface SearchBarProps {
    
}

const SearchBar = (() => {
    return (
        <div>
            
        </div>
    )
}) as React.FC<SearchBarProps>

export default SearchBar
